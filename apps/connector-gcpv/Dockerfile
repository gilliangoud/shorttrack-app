# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build connector-gcpv`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t connector-gcpv`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system connector-gcpv && \
          adduser --system -G connector-gcpv connector-gcpv

COPY dist/apps/connector-gcpv connector-gcpv
RUN chown -R connector-gcpv:connector-gcpv .

CMD [ "node", "connector-gcpv" ]
